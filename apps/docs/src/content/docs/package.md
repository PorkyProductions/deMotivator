---
title: 'demotivator package'
description: 'Reference and usage docs for the demotivator npm package.'
---

# demotivator package

The `demotivator` package contains the insult datasets and generation helpers used by the app and other consumers.

## Scope

- Pack-level insult organization.
- Reusable import surface for downstream projects.
- Typed APIs from the package `dist` output.
